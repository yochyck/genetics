import React, { useMemo, useState } from 'react';
import { saveGeneratedFlashcards, saveGeneratedQuizQuestions, saveUserDiseases, saveUserGlossaryTerms, saveUserMaterial } from '../store';
import { CourseSection } from '../types';
import { extractWithAI, generateFromAI, generateFromText } from '../ai/generation';
import { extractTextFromDocx, extractTextFromPdf, extractTextFromTxt, improveSectionsWithAI, prepareImportedMaterialDraft, SplitMode } from '../utils/importers';
export const ImportPage: React.FC<{navigateTo:(view:string,opts?:any)=>void}> = ({navigateTo}) => { const [title,setTitle]=useState('Новый материал'); const [text,setText]=useState(''); const [tags,setTags]=useState('user'); const [mode,setMode]=useState<SplitMode>('auto'); const [sections,setSections]=useState<CourseSection[]>([]); const [progress,setProgress]=useState(''); const [error,setError]=useState(''); const [aiStatus,setAiStatus]=useState(''); const headings=useMemo(()=>prepareImportedMaterialDraft(title,text||'',mode).headings,[title,text,mode]); const stats=useMemo(()=>({chars:text.length,words:(text.match(/\S+/g)||[]).length,sections:sections.length}),[text,sections]);
 const rebuild=()=>{ const draft=prepareImportedMaterialDraft(title,text,mode); setSections(draft.sections.map(s=>({...s,tags:tags.split(',').map(t=>t.trim()).filter(Boolean)}))); };
 const loadFile=async(file?:File)=>{ if(!file) return; setError(''); setProgress(`Чтение ${file.name}`); try{ const ext=file.name.split('.').pop()?.toLowerCase(); let extracted=''; let pages:any[]=[]; if(ext==='txt'||ext==='md') extracted=await extractTextFromTxt(file); else if(ext==='pdf'){ const r=await extractTextFromPdf(file,setProgress); extracted=r.text; pages=r.pages; } else if(ext==='docx') extracted=await extractTextFromDocx(file); else throw new Error('Поддерживаются .txt, .md, .pdf, .docx'); const draft=prepareImportedMaterialDraft(file.name.replace(/\.[^.]+$/,''),extracted,mode,pages); setTitle(draft.title); setText(draft.text); setTags(draft.tags.join(', ')); setSections(draft.sections); setProgress('Готово'); }catch(e){ setError(`Не удалось прочитать файл: ${e instanceof Error?e.message:String(e)}. Вставьте текст вручную или установите pdfjs-dist/mammoth локально.`); setProgress(''); } };
 const mergeWithPrevious=(i:number)=>{ if(i<=0) return; const next=[...sections]; next[i-1]={...next[i-1],content:`${next[i-1].content}\n\n${next[i].content}`}; next.splice(i,1); setSections(next); };
 const splitSection=(i:number)=>{ const s=sections[i]; const half=Math.floor(s.content.length/2); const at=s.content.indexOf('\n\n',half); const pos=at>0?at:half; const next=[...sections]; next.splice(i,1,{...s,content:s.content.slice(0,pos).trim()},{...s,id:`${s.id}-split`,title:`${s.title} (часть 2)`,content:s.content.slice(pos).trim()}); setSections(next); };
 const ai=(kind:'terms'|'diseases'|'flashcards'|'quizzes'|'summary'|'improve'|'course')=>async()=>{ setAiStatus('ИИ-обработка через backend...'); if(kind==='improve'){ const result=await extractWithAI(text,['sections'],{title,fileType:'manual'}); if(result.sections.length) setSections(result.sections as CourseSection[]); else setSections(await improveSectionsWithAI(text,sections)); setAiStatus(`Разбиение: ${result.provider} ${result.model||''}${result.fallbackUsed?' · fallback: '+(result.fallbackReason||'chain'):''}; sections=${result.sections.length||sections.length}`); return;} const task=kind==='quizzes'?'quiz':kind==='course'?'summary':kind; const extracted=await extractWithAI(text,[task],{title,fileType:'manual'}); if(kind==='terms') saveUserGlossaryTerms(extracted.terms as any); else if(kind==='diseases') saveUserDiseases(extracted.diseases as any); else if(kind==='flashcards') saveGeneratedFlashcards(extracted.flashcards as any); else if(kind==='quizzes') saveGeneratedQuizQuestions(extracted.quiz as any); else if(kind==='course'||kind==='summary') saveUserMaterial({id:crypto.randomUUID(),title:`Конспект: ${title}`,rawText:extracted.summary||JSON.stringify(extracted),processedSections:[],tags:['ai','summary'],createdAt:Date.now(),updatedAt:Date.now()}); setAiStatus(extracted.apiAvailable?`AI ${extracted.provider} ${extracted.model||''}${extracted.fallbackUsed?' · fallback: '+(extracted.fallbackReason||'chain'):''}; terms=${extracted.terms.length}, cards=${extracted.flashcards.length}, quiz=${extracted.quiz.length}`:`API недоступен, сохранён локальный fallback: ${extracted.error||''}`); };
 const save=(actions:string[])=>{ const processed=sections.length?sections:prepareImportedMaterialDraft(title,text,mode).sections; const finalTags=tags.split(',').map(t=>t.trim()).filter(Boolean); const mat={id:crypto.randomUUID(),title,rawText:text,processedSections:processed.map(s=>({...s,tags:finalTags})),tags:finalTags,createdAt:Date.now(),updatedAt:Date.now()}; if(actions.includes('material')||actions.includes('course')||actions.includes('source')) saveUserMaterial(mat); if(actions.includes('terms')) saveUserGlossaryTerms(generateFromText('terms',text) as any); if(actions.includes('cards')) saveGeneratedFlashcards(generateFromText('flashcards',text) as any); if(actions.includes('tests')) saveGeneratedQuizQuestions(generateFromText('quizzes',text) as any); if(actions.includes('assistant')) navigateTo('assistant',{query:title}); else navigateTo('material'); };
 return <div className="page import-page max-w-7xl mx-auto space-y-5"><h1 className="page-title text-3xl font-bold">Импорт материалов PDF/DOCX/TXT</h1><div className="import-layout panel bg-white rounded-3xl border p-5 grid lg:grid-cols-2 gap-5"><div className="space-y-3"><input value={title} onChange={e=>setTitle(e.target.value)} className="w-full border rounded-xl p-3"/><input value={tags} onChange={e=>setTags(e.target.value)} className="w-full border rounded-xl p-3" placeholder="теги через запятую"/><select value={mode} onChange={e=>setMode(e.target.value as SplitMode)} className="w-full border rounded-xl p-3"><option value="auto">auto</option><option value="byHeadings">byHeadings</option><option value="byParagraphs">byParagraphs</option><option value="byPages">byPages</option><option value="manual">manual</option></select><input type="file" accept=".txt,.md,.pdf,.docx" onChange={e=>loadFile(e.target.files?.[0])} className="w-full"/><button onClick={rebuild} className="btn-primary">Разбить на разделы</button>{progress&&<p className="text-sm text-indigo-600">{progress}</p>}{error&&<p className="text-sm text-rose-600 bg-rose-50 rounded-xl p-3">{error}</p>}<div className="bg-slate-50 rounded-2xl p-3 text-sm">Символы: {stats.chars}; слова: {stats.words}; разделы: {stats.sections}<br/>Заголовки: {headings.slice(0,6).join(' • ')||'не найдены'}</div><textarea value={text} onChange={e=>setText(e.target.value)} className="w-full min-h-80 border rounded-2xl p-4" placeholder="Вставьте текст методички или материала"/></div><div className="space-y-3"><h2 className="font-bold text-xl">Предпросмотр разделов</h2>{sections.length===0&&<p className="text-slate-500 bg-slate-50 rounded-2xl p-5">Загрузите файл или вставьте текст и нажмите “Разбить на разделы”.</p>}{sections.map((s,i)=><div key={s.id} className="border rounded-2xl p-3"><input value={s.title} onChange={e=>{const n=[...sections]; n[i]={...n[i],title:e.target.value}; setSections(n)}} className="block w-full border rounded-xl p-2 mb-2"/><textarea value={s.content} onChange={e=>{const n=[...sections]; n[i]={...n[i],content:e.target.value}; setSections(n)}} className="w-full min-h-28 border rounded-xl p-2 text-sm"/><div className="flex gap-2 mt-2"><button onClick={()=>mergeWithPrevious(i)} disabled={i===0} className="btn disabled:opacity-50">Объединить</button><button onClick={()=>splitSection(i)} className="btn">Разделить</button><button onClick={()=>setSections(sections.filter((_,idx)=>idx!==i))} className="btn-danger">Удалить</button></div></div>)}</div></div><div className="panel bg-white rounded-3xl border p-4"><h2 className="font-bold mb-3">ИИ и сохранение</h2><div className="flex flex-wrap gap-2"><button onClick={ai('improve')} className="btn">ИИ: улучшить разбиение</button><button onClick={ai('terms')} className="btn">ИИ: извлечь термины</button><button onClick={ai('diseases')} className="btn">ИИ: извлечь болезни</button><button onClick={ai('flashcards')} className="btn">ИИ: создать карточки</button><button onClick={ai('quizzes')} className="btn">ИИ: создать тесты</button><button onClick={ai('summary')} className="btn">ИИ: сделать конспект</button><button onClick={ai('course')} className="btn">ИИ: добавить в курс</button></div>{aiStatus&&<p className="text-sm text-slate-600 mt-3">{aiStatus}</p>}<div className="flex flex-wrap gap-2 mt-4"><button onClick={()=>save(['material'])} className="btn-primary">Сохранить материал</button><button onClick={()=>save(['source'])} className="btn">Добавить как источник</button><button onClick={()=>save(['course'])} className="btn">Добавить в курс</button><button onClick={()=>save(['terms'])} className="btn">Сохранить термины</button><button onClick={()=>save(['cards'])} className="btn">Сохранить карточки</button><button onClick={()=>save(['tests'])} className="btn">Сохранить тесты</button><button onClick={()=>save(['assistant'])} className="btn">Отправить в ИИ</button></div></div></div> };
